# G12 prospectively selected independent natural-history case
Frozen at 2026-09-20 12:21 UTC BEFORE inspecting the target commit patch or running verification.

Primary repository: https://github.com/olofk/serv (independent of ZipCPU and RISU).
Selection event: public GitHub Actions formal workflow lists a natural historical change, `41e8aee` (full 40-character SHA to be resolved), titled "Fix zero register handling in servile_rf_mem_if module"; automated formal verification #185. Selection based only on public workflow name and change title, not experiment outcome or patch contents. Compare exact commit to its first parent; preserve all original RTL and available original proof flows. If first-parent change is not a meaningful isolated RTL change, record exclusion and do NOT substitute a favorable result.

Predeclared question: Does the available repository-native formal signoff distinguish pre/post zero-register behavior? Does actual runnable behavior under legal inputs differ from spec (RISC-V x0 must read zero and writes ignored)? If formal PASS both despite a reproducible spec violation, is that due to coverage, assumptions, or a genuine proof-obligation gap? If original formal fails post, or tools missing, report that rather than manufacturing LOSS. Describe unit/environment contracts and what reviewer action follows.

Primary endpoints (independent of outcome): (A) exact source pin, diff, and reproducible original formal build status parent and child (PASS/FAIL/TIMEOUT/NOT RUN), (B) architecture x0 invariant test with genuine transactions, (C) supported witness or proof under native hierarchy, (D) if A and B permit: same preserved assertions/assumptions under first-parent/post compare, with directed assertion-gap localization. Negative controls: no-write or an unrelated register; corrected version should satisfy same architecture property. Truth table must retain UNKNOWN distinctly from UNSAT. Collect provenance SHA256 and entire logs. No autogenerated hand-asserted internal state may be represented as an externally reachable input.

Novelty gate: No "new bug" credit for a publicly described historical fix; no novel algorithm credit for checking x0 or comparing PASS counts. Research contribution only if demonstrated new consequential decision beyond original formal/checks with a reproducible end-to-end provenance, contrasted with simple regression tests, original formal checker, and direct assertion of x0. If simpler baseline suffices, record that as a negative finding.

Fallback only on documented inability to access/execute primary: (1) select a pre-2026 natural hardware/formal fix in YosysHQ/picorv32 by earliest eligible commit date under fixed criteria "single RTL or verification file plus native formal target"; (2) a pre-2026 natural Ibex formal-interface fix. Freeze each exact commit and expected outcomes before simulation, never choose a candidate based on a desired SAT/UNSAT result. The original selected candidate remains in the report if negative.