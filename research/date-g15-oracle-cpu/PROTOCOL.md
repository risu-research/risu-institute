# G15 prospective integration protocol — frozen before execution (2026-09-20)

Known public G14 defects #30/#49 are NOT new bug discoveries. We have already seen original DIV/REM outputs. New question: can the wrong original formal oracle demonstrably reject a *real, unmodified, correct CPU* under legal software, and what additional reviewer decision follows beyond a one-vector ISA test?

Exact original DUT: YosysHQ/picorv32 commit de92ce54e8a3f24f2adc6b5d04de35e4edb873e5, unchanged `picorv32.v`, `ENABLE_DIV=1` with native PCPI division; original golden checker: YosysHQ/riscv-formal c992aa61fdfe0846c5ed90324c596202a1c69b76, unmodified `insns/insn_div.v` and `insns/insn_rem.v`. Fixed checker separately named, changing only signed expression parentheses. Real user-level program runs through CPU memory bus, no DUT internal forcing: ADDI x1,-1; ADDI x2,2; DIV x3,x1,x2; REM x4,x1,x2; SW results; then DIV/REM for 7/3 and -7/3 as positive and signed controls, SW results, EBREAK. Report exact opcodes/PC/bus stores and x result with actual checker results, independent ISA arithmetic, corrected checker, all original SHA and tool versions.

Predeclared endpoints: real CPU signed cases PASS/FAIL/UNKNOWN by independent ISA; original oracle false rejection count; corrected oracle false rejection count; positive/positive control; no synthetic DUT accepted as a real hardware bug. Explicitly distinguish native simulation from native upstream formal and abstain from novel algorithm claim when direct ISA regression catches the issue. Preserve failed runs and source/CI artifacts, never convert NOT RUN/TIMEOUT into PASS. Branch is a research branch, not upstream bug filing.