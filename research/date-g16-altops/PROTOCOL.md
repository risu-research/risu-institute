# G16 prospective diagnostic: alternative operations are not production DIV/REM evidence

2026-09-20. Read original upstream checks.cfg and documentation before test. ALTOPS is PUBLIC and INTENTIONAL, not a newly found bug. Use the exact unchanged historical PicoRV32 `de92ce54e8a3f24f2adc6b5d04de35e4edb873e5` and original checker source at `c992aa61fdfe0846c5ed90324c596202a1c69b76`; run exactly the same signed legal three-case G15 CPU program but with `RISCV_FORMAL_ALTOPS` defined on both DUT and ISA checker. Save native fetch/store and both old and signed-corrected checker outputs. Before seeing results, hypothesize that the old and corrected checkers will agree with the abstracted CPU and the results will not agree with actual ISA. A failure or timeout is a real negative, never PASS. This is native simulation of the documented alternative operation mode, NOT a native upstream formal proof, and not an unsoundness claim about its intended control/path abstraction. Strong baseline is trivial: read config define, documentation, and preprocessor branch. New verification algorithm NOT assumed. Record exact SHA and first failed attempt.