# G20 preregistration arithmetic erratum (added after first execution)

The initial preregistration explicitly enumerated two revisions x two register configurations x two shifter implementations x three instruction families x five shift amounts; product 2*2*2*3*5 = **120**, not the '60' incorrectly typed in its parenthesis. The first actual GitHub run 35518301987 **ran and archived all 120 cells** before hitting the final wrong `assert len(data)==60` (result file MATRIX.csv is intact; runner's overall workflow result was success because the job intentionally archived failed test scripts). No outcome was discarded or added. Its 120 valid cells include 18 mismatches, all confined to old source RV32E high shifts. We amend ONLY that accounting assertion to 120, rerun the exact source-pinned full matrix, preserve both executions and report the first fixture error. The original protocol remains immutable.