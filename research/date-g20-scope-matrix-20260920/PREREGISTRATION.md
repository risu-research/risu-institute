# G20 fixed evaluation protocol — 2026-09-20

This is a FOLLOW-ON to known G13 results, NOT prospective selection of the historical fix. Historic PicoRV32 first-parent b91af1c87e8bc4ae5b0b438c4ac04400a12b76fb; child de92ce54e8a3f24f2adc6b5d04de35e4edb873e5; riscv-formal framework c992aa61fdfe0846c5ed90324c596202a1c69b76. Original source bytes remain unchanged. Do not infer original 2024 CI status.

Predefined native denominator: all three RV32I shift-immediate families SLLI, SRLI, SRAI, at shift amounts 0,15,16,17,31, for parent and child, ENABLE_REGS_16_31=0 and =1, BARREL_SHIFTER=0 and =1 (60 executable CPU runs), plus explicitly labelled ADDI no-shift control. Each run starts from reset, supplies a legal RV32E instruction via CPU memory bus, observes actual store; no force of DUT registers or decoded instruction. Initial source operand is 0x80010001, which distinguishes sign preservation and lower shift amount effects. All 60 cells count, including invalid, timeout, crash, and no store. Independent ISA expected values use 32-bit Python integer model; compare unmodified parent and child source hashes. An earlier 12-instruction directed example is NOT part of this new denominator.

Formal denominator: at minimum original SLLI instruction checker for both versions in RV32I, RV32E unqualified and RV32E qualified; attempt SRLI and SRAI with the SAME legal-register qualification and pinned framework, depth 20, no selective dropping of failed jobs. Record SBY status (PASS, FAIL, ERROR, TIMEOUT, NOT_RUN) separately from workflow status; decode failure traces, filter illegal register operands before calling any hardware defect. Some checker ISA is rv32imc; qualification restricts retired shift-immediate rs1, rd to x0..x15, never shamt[4]. This remains a diagnostic subset and is NOT an unmodified RV32E checker.

Non-vacuity first: run actual original CHILD RTL with a formal cover of RVFI-valid SLLI x2,x9,16 (and x2,x9,15 negative boundary control), legal rs1 and rd. When possible run same property on parent; record reachability depth/cover status and trace. Distinguish a generic wrapper cover from an exact instruction cover; never claim the bounded child PASS is nonvacuous unless the covered instruction occurs in a constraint-consistent trace under a matching checker environment. Independently record whether any assumptions restrict rvfi output rather than instruction input.

Primary evaluand: per instruction task, when does source/file inspection, parameter inspection, directed CPU execution, original formal, then qualifier change or cover change a justified signoff decision? Count judgments, NOT independent bugs or distinct independent projects. Report cheap baselines with no claimed speedup. If original source or original task fails to compile, retain unchanged source and stop that branch. If some formal checks cannot run or timeout, retain ERROR/UNKNOWN, not PASS. Document every fixture correction and failed run; do not replace cases, adjust numerator, or retroactively label the protocol blind.