# G22 add-on: post-result CSR configuration cross-check — registered before this run

The original frozen SERVing SoC prediction was wrong: exact original source full SoC WITH_CSR=1 yielded x31=7 and x1=1 for BOTH parent and child, despite a module-level G12 x31 bug under rf_regs=32. Preserve these outcomes and the failed parent prediction assertion; do not replace data. Source diagnosis: `serving` sets `regs=32+WITH_CSR*4`; `servile_rf_mem_if` sets `rf_depth=$clog2(rf_regs*4)`. WITH_CSR=1 => rf_regs=36, rf_depth=8; predecessor's erroneous `&i_raddr[rf_depth-1:2]` does not classify x31 (0x7c, bits7:2=011111) as zero. WITH_CSR=0 => rf_regs=32, rf_depth=7; x31=0x7c bits6:2=11111 triggers false zero. This configuration hypothesis follows observing first SoC results, NOT blind or pre-registered G12 selection.

Freeze 2 x 2 original SoC study: both original source revisions (838d16615e81786062fae84b9f7c044fb8f6bbe4 -> 41e8aeedfd1e9ad5f95902c5b0dfc83d1c99e5d2), WITH_CSR=0 and WITH_CSR=1, identical existing 6-word legal instruction program and same original SoC architecture and externally acknowledged 32-bit store. x31 target expects: parent CSR0=0 (fault), child CSR0=7, both CSR1=7. x1 negative control=1 in all four. All four must compile and complete, record ERROR/TIMEOUT otherwise. No RVFI or G18 debug mirror in this experiment. Do not count 4 tasks as four independent defects. Control for alternative explanation by verifying actual RAM x31 written 7 when predecessor CSR0 output is zero if feasible without manipulating DUT state; at minimum unchanged x1 and original G12 module write/read evidence are independent.

Source-selected original formal target `serv.core` still excludes modified `servile_rf_mem_if.v`; a fileset grep is sufficient to withhold transfer of original PASS and remains the cheapest baseline, not a new solver capability. Do not claim historic formal was wrong. The substantive opportunity is demonstration of an actual product-configuration-dependent architectural consequence in a second design, with a falsifying negative control. If posthoc configuration hypothesis fails, state failure; retain original G21 paper unchanged unless genuine new evidence warrants a bounded addition.
