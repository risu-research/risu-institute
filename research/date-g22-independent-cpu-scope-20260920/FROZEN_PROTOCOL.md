# G22 frozen protocol — independent SERV CPU-level scope test

Date 2026-09-20. Extends already known, G18-preregistered historical SERV debug CSR edit; NOT a new blind discovery nor an independent replication of PicoRV32 RV32E. The prior G18 module-level CSR and ordinary ADDI formal results are known. Our NEW prediction is that a legal end-to-end SERV CPU program targeting the affected debug CSR will distinguish original parent vs child, while the unchanged listed instruction formal task does not cover that behavior. A successful full CPU result plus a legal, purpose-specific property/functional test would elevate existing independent control from module state observation to architectural consequence. This is a falsifiable prediction, not a predetermined outcome.

Freeze source selection: olofk/serv parent 1e4ea0527e0c7a3737bc501aa5d93bb16508ec15, successor selected by original G18 pinned child or immediate historical child as recorded in G18 archive; pin its full SHA before executing, no substitute commits. Source files untouched. Use actual SERV CPU/top-level with instruction memory and appropriate debug CSR interface, reset, program fetch, legal CSRRS/CSRRW or other supported instruction directed at mscratch if supported. Follow system bus and register file with no force of DUT internal state. Compare architectural observed result (e.g. subsequent CSR read and store) using independent RISC-V ISA expectation and a negative control (ordinary ADDI) under same harness. Parent/child must run identical program, initial conditions, and environment. If this SERV debug block exposes mscratch only via debug protocol and not architecturally via standard CSR instructions, record that and SWITCH to a source-grounded end-to-end debug access observable at SERV core interface; do not call it ISA behavior. If complete integration is unsupported, retain original module level and do not upgrade G21 claims.

Five stages for this one chosen independent pair: (1) exact source inclusion; (2) effective debug/CSR configuration and reachable interface; (3) smallest legal end-to-end program plus negative control; (4) original historical workflow's actual selected instruction formal tasks on parent and child, plus purpose-specific assertion or cover only if correctly scoped and original checker compatible; (5) explicit review decision and cost measured by reproducible command-duration tool segments, no human-time savings claim. CI status never substitutes SBY status. Do not assume unchanged original historic CI success; modern rerun labelled modern. Retain all fixture attempts, compilation errors, timeouts, and nulls with source hashes and raw traces. Outcome must not be called multiple independent bugs.

Paper inclusion gate: require *real CPU-level or externally observable top-level* parent-child divergence attributable to the documented source fix, matching legal external operation, full matching controls, and an unchanged formal task genuinely not checking it. Otherwise retain as negative/control only. No change to G21 PDF until audit clears; retain G21 immutable. A distinct higher-impact fallback, conditional on failure, is a source-pinned mutation/legality sensitivity test of PicoRV32's existing formal checker, with explicit mutation outcomes; such fallback cannot be counted as second independent core.
