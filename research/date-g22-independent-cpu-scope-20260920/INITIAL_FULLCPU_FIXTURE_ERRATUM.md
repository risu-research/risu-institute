# G22 first full-CPU result and predetermined harness repair

Original full-core run 35521392389 compiled both exact pinned historical sources and retired legal ADDI/CSRRW/CSRRWI/CSRRS/SW instructions. Both observed stores `X0000002` at address 0, `00000001` at address 4; the unknown nibble prevented an honest 32-bit architectural correctness claim and caused the timeout assertion, despite apparent lower bits. The child debug mirror was sampled at the CSR instruction's `rvfi_valid` event BEFORE the one-cycle delayed shadow update, so both appeared `00000001` at that cycle; this is an invalid instant for comparing the successor shadow. Preserve both original raw failed ZIPs. Fix only the test environment with the upstream-defined `SERV_CLEAR_RAM` macro for known-zero RF initial state, and sample both debug shadow values ~5 cycles after CSR-only retirement, prior to the following CSR read. Same original source hashes, same exact program/instruction bus and target, same parent and child. If the architectural 32-bit store remains unknown, report UNKNOWN; do not mask bits to call it a success. Retain all new failures. These are testbench/setup repairs, NOT evidence of CPU source bugs.