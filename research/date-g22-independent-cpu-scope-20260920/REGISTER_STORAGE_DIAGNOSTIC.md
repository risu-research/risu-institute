# G22 passive physical RF memory probe (post-hoc source-driven diagnostic)

After four successful original full-SoC runs of same known G12 historical source, CSR0 old x31 external store=0 while child store=7, and CSR1 both=7, x1 all=1. This follow-up DOES NOT select new cases; it adds a read-only probe to the four already executed SoC configurations to determine whether old CSR0 zero comes from a failed write or the known changed RF read zero-gating. `servile_rf_mem_if` `rf_raddr=~{{aw-rf_depth{1'b0}},i_raddr}`. For `serving.memsize=8192`, aw=13; x31 byte0 logical register-file address=31*4=124; raw SRAM index=8191-124=8067. In each of four exact same legal instruction programs, after the two external stores, passively sample `dut.ram.mem[8067]` (plus adjacent bytes), and print expected source-specific value. Prediction: physical byte0 is 7 for ALL four, even when old CSR0 CPU external output is 0. If false, retain mismatch and revise causal interpretation. This read-only simulator observation is *not* a memory write or CPU register force; it is supplementary source-level localization, not architectural interface evidence by itself. Archive unchanged original DUT SHA and generated modified *testbench* for each case. This is post-outcome follow-up, not blind selection, and does not alter completed primary 2x2 matrix.