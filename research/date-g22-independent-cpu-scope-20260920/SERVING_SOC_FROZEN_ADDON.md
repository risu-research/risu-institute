# G22 independent SoC architectural follow-up — frozen before execution

DATE 2027 research, 2026-09-20. This is a FOLLOW-ON to the already known G12 SERVile module-level history; not discovery of a new historical bug. Historical original repository olofk/serv; parent `838d16615e81786062fae84b9f7c044fb8f6bbe4`, child `41e8aeedfd1e9ad5f95902c5b0dfc83d1c99e5d2`. Only `servile/servile_rf_mem_if.v` changed, zero-register decoder. Original `serv.core` formal excludes the modified file; simple file membership ALREADY diagnoses absence of its verification; no fancy discovery advantage. Original historical CI PASS/PASS does not prove serving system. Preserve pins and no source change.

The module under test is actually instantiated in `serving/serving.v` as part of `serving` SoC, NOT in `servile/servile.v` alone. `serving` includes original SERV CPU, servile wrapper/mux/arbiter, shared serving_ram and servile_rf_mem_if. Primary direct architectural target: drive an identical legal CPU instruction program via original serving_ram instruction bytes (never force any CPU/RF signals). Program writes known value 7 into x31, stores x31 to high-address external wishbone peripheral 0x40000000; control writes known 1 to x1 and stores x1 to 0x40000004. Observe complete externally acknowledged CPU output transactions, compare independent ISA expected 7,1, parent vs child. Preserve unknowns and invalid; NO counting simple manual RF module tests as complete CPU. If full CPU cannot execute, retain the module result and report no SoC promotion.

Primary hypothesis: predecessor x31 read is zero despite previous full instruction write, successor external store 7; x1 control 1 in both. If contrary, retain counterevidence unchanged and do not amend case to obtain desired answer. Initialize the external program memory identically in both revisions, and record all program hex, wrapper params, source hashes, compile errors and waveform/logs. Distinguish data-path error vs observer bug (the separate G22 SERV debug monitor result). Original CI is out-of-target on this changed module; do NOT claim this work reveals a hidden failure of covered original formal obligations. A useful negative control is exact source fileset grep correctly withholding original PASS transfer, and explicit added module assertions from G12 parent fail/child pass. Measurements include CPU cycle bounds and tool wall time (not human review time).

Paper inclusion threshold: genuine original SoC architectural outputs confirmed, identical legal program, independent expected outputs and matched controls. Even if true, count as one historical fix in one additional hardware design, not independent sample of 120 cells or evidence of new formal algorithm. Retain G21 as main design story, use SoC as compact independent scope control only if it changes a justified review decision. Preserve all fixture failures.
