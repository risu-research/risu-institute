# G22 pre-execution source-grounded pivot

Source-only inspection after G22 preregistration, before any G22 full-core experiment, reveals original `serv_top.v` instantiates `serv_csr` (actual CSR datapath) and separately `serv_debug` under `if (DEBUG)` for debug shadow state / RVFI instrumentation. Original e97bdaf -> 1e4ea052 edit is in `serv_debug.v` only. Thus a parent-child difference in actual CSR instruction execution is no longer a justified prediction: architecture can be identical while the DEBUG observer differs. DO NOT claim G18 module-level dbg_mscratch is real architectural CSR. Do not replace pinned case.

Revised high-information test on the SAME frozen pair: at exact source pins compile original full SERV core with DEBUG=1, WITH_CSR=1 and RISCV_FORMAL=1. Drive a legal CSR program using instruction bus, observe external CPU program output / actual CSR storage, compare against debug mirror and RVFI outputs when valid. Retain an ADDI program control and the G18 standalone debug module witness. If original RTL dependency complexity blocks full-core reproduction, run a source-to-output cone-of-influence audit and bounded sequential miter of the non-RVFI architectural outputs, NOT an invented CPU execution. Structural proof that changed module has no feedback to datapath is a useful negative, but do not label it an end-to-end CPU simulation. The five-stage review records earliest point the wrong inference can be rejected (source direction), proof/checker scope, real measured segment cost. Any G22 paper strengthening must state exact tested claim and distinguish observer divergence from DUT malfunction.

Submission threshold: if no demonstrable externally observable or valid RVFI impact, exclude SERV from core contribution and keep in research log only. If it changes RVFI on a valid instruction without changing architectural result, show matched relevant trace / model and describe an instrumentation-scope effect only, not new CPU bug or recovered original CI. All null/error outcomes included in raw archive.