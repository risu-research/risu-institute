# G22 v2 child script assertion erratum — preserve raw before v3

Run 35521528132 at original exact sources with upstream `SERV_CLEAR_RAM` obtained TWO valid architectural stores for both historical versions: `0x00000002` (read back mscratch after CSR-only write), `0x00000001` (ADDI control). The debug monitor at cycle 111 is `0x00000001` parent and `0x00000002` child as expected. Parent run exits 0. Child emits success-form architectural observations but ends $fatal at *obsolete initial-test flag* `seen_newcsr`: original test sets flag at CSR-only rvfi_valid cycle 105, BEFORE delayed new shadow update, so flag remains 0 despite source-correct cycle-111 sample. Do not call child v2 a successful fixture; preserve failure. V3 changes ONLY redundant final assertion to check final shadow directly, retains exact source pins, same legal full CPU program, RF init, cycle-111 assertion. If the rerun passes, label initial versions failed fixture and v3 the accepted CPU run. This is not a change to DUT or result selection.