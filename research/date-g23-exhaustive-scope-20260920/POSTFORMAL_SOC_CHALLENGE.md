# Post-formal challenge, pre-run freeze (2026-09-20)

G23 completed an initial symbolic four-by-four contract; the old module fails the zero-register obligation at both CSR settings. This follow-up test is NOT preregistered independently of the formal outcomes and is not a randomly selected new design. Hypothesis: the original G22 full SoC initialized *every RAM byte to zero*, masking an x0-read defect when CSR=1. Test the causal prediction without editing any original upstream RTL.

Original SERV/SERVing parent 838d16615e81786062fae84b9f7c044fb8f6bbe4, child 41e8aeedfd1e9ad5f95902c5b0dfc83d1c99e5d2, each WITH_CSR in {0,1}, original serving top, servile and SRAM. Identical valid RISC-V ROM of six words from G22: LUI x5,0x40000; ADDI x31,x0,7; SW x31,0(x5); ADDI x1,x0,1; SW x1,4(x5); JAL x0,0. Retain original G22's zero-initialized ROM/SRAM as baseline. This test ONLY changes simulator initial value of the four SRAM byte cells physically mapped to x0: x0 backing word = 0x00000020 (nonzero low byte). Do not force CPU, RF output, raddr, data buses, CSR or architectural x0 signals. The user-facing external Wishbone bus is simulated by the testbench, not a physical peripheral. Log initial/final x0 SRAM byte, precisely two full-word external transactions, no duplicate stores, observed architectural words, and correct base address. If both CSR modes fail to execute, retain timeouts and stop, don't replace operands.

Falsifiable prediction before this SoC run: parent/CSR0 x31 external store remains 0 because old x31 masking also applies, parent/CSR1 x31 external store becomes 0x27 (39) because old x0 leaks 0x20 and x31 is not masked; parent both CSR modes x1 external store becomes 0x21 (33). Child both CSR modes x31=7 and x1=1. Any deviation must be investigated and all four cells retained. This test proves a concrete original-SoC execution under a chosen legal but nonzero backing RAM initialization, not behavior across all possible power-on memories or silicon. Source hashes and script must be archived. Compare the exact original G22 all-zero baseline, revise any paper wording implying that old CSR-on behaves correctly for every possible initialization. No new independent defect count.