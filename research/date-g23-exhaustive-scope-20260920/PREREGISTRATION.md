# G23 frozen experiment — 2026-09-20

Purpose: test whether G22's sampled independent SERVing result admits a source-pinned, exhaustive *module-interface* configuration boundary, so an engineer can distinguish proof-target exclusion, actual system failure, and the exact qualifying configuration. This does not prove a complete CPU or discover a new bug. NO changes to upstream DUT RTL; select exact parent 838d16615e81786062fae84b9f7c044fb8f6bbe4 and child 41e8aeedfd1e9ad5f95902c5b0dfc83d1c99e5d2. Verify the one changed module and source hashes.

Two-by-two revisions x WITH_CSR=0,1. For each, three fixed read obligations using the unchanged original `servile_rf_mem_if.v` and modeled stable SRAM byte (symbolic, unconstrained): (A) register zero x0 must read 0 for any byte index and any RAM byte, (B) x30 must read the supplied RAM byte, (C) x31 must read supplied RAM byte. Also one general read obligation per cell: arbitrary register 0..31 and 2-bit byte index must read 0 iff register=0, otherwise supplied byte. An arbitrarily fixed symbolic SRAM input models a stable, returned byte and *does not* independently establish whole-CPU reachability or a complete shared SRAM timing proof. All 16 property cells, regardless of outcome, are retained. Use fixed SBY depth 4 with smtbmc z3; an ERROR/TIMEOUT is not a pass; if engine unsupported, use original Icarus HDL simulation exhaustive over all 32 registers, 4 byte lanes, 3 payloads 0x00,0x5a,0xff (384 samples per cell) but call that exhaustive only over this finite sample space, never an all-operand proof.

Expected falsifiable result (not claimed as data): parent x0 FAIL with both CSR settings, parent x31 FAIL only CSR OFF, x30 PASS throughout; child ALL PASS. Source-based explanation: parent uses an all-ones test on high RF address bits; child uses all-zero test. Independent controls: x30 adjacent; nonzero SRAM value essential to observe x0 and x31; no Wishbone bus accesses during probe. Retain original SBY config, statuses, actual witness bit fields, simulator stdout if used. If x0 fails even on repaired source, revisit harness and stop interpretation. A property PASS, if achieved, proves only the local mapping for the bounded transition(s) and symbolic stable SRAM return; do not call it CPU ISA compliance or a proof that historic SERV formal missed a target that it did not include.

Promotion gate: only change G22 manuscript if a complete 4x4 result, genuine original source, and distinct decision-relevant scope conclusion are verified. Otherwise report as supplementary/negative and preserve G22 as latest paper. No performance speedup, independent bug count, human audit or universal proof claims.