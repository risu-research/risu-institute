# G24 original SERVing SoC x0 lane/carry falsification — frozen before run, 2026-09-20

Question: Is G23's x0 backing-memory leak restricted to the low byte and chosen 0x20 value, or does it affect each of four architectural x0 byte lanes and carry-sensitive operands? This is an exploratory follow-on to G23, NOT a prospectively discovered historical defect and NOT a new core. Retain exact original upstream SERV commits parent 838d16615e81786062fae84b9f7c044fb8f6bbe4 and child 41e8aeedfd1e9ad5f95902c5b0dfc83d1c99e5d2. No changes to DUT RTL. Reuse G23 exact 6-instruction program and external Wishbone testbench, RESET_STRATEGY MINI, 8192-byte SRAM, independent x31 and x1 stores. All SRAM zeroed except unchanged ROM and ONE byte at address MEMSIZE-1-lane (lane=0,1,2,3). Perturbation values 0x20 and 0xff. Four design cells (parent/child x CSR OFF/ON) x 4 lanes x 2 values = 32 fixed runs, all retained including errors/timeouts. Each case must compile and produce exactly two full-width stores at expected bus addresses with a stable injected byte after completion. Unexpected result is a recorded FAIL; do not substitute a successful case. Baseline G23 low-byte/0x20 is one repeat, not new independent evidence.

Reference calculation: B=(byte << (8*lane)) modulo 2^32, expected repaired external stores x31=7 and x1=1. Hypothesis for old CSR ON: x31=(B+7) mod 2^32, x1=(B+1) mod 2^32. Hypothesis for old CSR OFF: x31=0 from separate x31 mask, x1=(B+1) mod 2^32. Distinguish emitted bus observations from inferred architectural register values. Lane0/0xff creates carry into bit 8; lane3/0xff exercises bit31. x30 adjacent-read control is covered by G22; no new independent failure asserted. If any case differs, preserve source, ROM, trace and counterexample, do not hide behind CI green.

Promotion gate: update G23 manuscript only if a previously unsupported, decision-relevant result emerges (e.g. existing initial-state claim must be corrected, or a qualitatively new correctness boundary). If all 32 merely follow the source-predicted pattern, archive as robustness/supplement and preserve G23 main paper unchanged. Do not count 32 tests as independent bugs or imply all 2^32 initial states, actual silicon, or new formal coverage. This program cannot prove portability beyond two historical revisions.