# G25 final audit: frozen before this follow-up execution, 2026-09-20

Evidence from completed G25 A-D is **already known**. This is a retrospective *auditing and baseline* task, not a prospective target discovery. Preserve all earlier failures and their repair sequence. Original fixed upstream commit: ZipCPU/wbuart32 d8ea86d1b8ef47d0b1c13f291a958b6aac9229f9. Do not alter DUT RTL. Reproduce archived symbolic harness unchanged for alternate-solver BMC with `smtbmc boolector` where available; record unavailability/error rather than claiming PASS. Separately create a **native-only runtime input adapter** to substitute each of 256 values for the fixed literal 0xa5, compiling original physical UART once, then run all 256 payloads with vvp using one legal AXI AW/W write at the same fixed steps. Require exactly one AXI handshake, eight sampled wire bits equal the supplied payload, and recorded completion per input. This adapter changes only testbench payload assignment, not any original DUT source, protocol, timing, or oracle. Retain full output and failures, source hashes, SBY status and logs. Freeze promotion gate: if exhaustive native test takes similarly little work and independently establishes same bounded finite-domain claim, G25 cannot be sold as demonstrating a unique decision over a feasible simple baseline; leave the G23 manuscript unchanged. Distinguish strong proof of *fixed-schedule byte correspondence* from unbounded UART correctness, novel historical defect, novelty of conventional formal properties, upstream CI history, and independence of maintainer (ZipCPU is also maintainer of previous ZipCPU project).