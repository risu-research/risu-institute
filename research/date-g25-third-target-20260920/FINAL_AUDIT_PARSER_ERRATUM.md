# G25 final native baseline parser erratum — before corrected rerun, 2026-09-20

First final-audit workflow run 35529884975 successfully compiled the *unmodified* original UART and ran all 256 payloads; every vvp process exited 0 and its raw output contains exactly one `FRAME_FULL_NATIVE_DONE accepted=1 sampled=XX byte=XX` followed by Icarus' `$finish` diagnostic on a later line. The aggregation script used `re.compile('^FRAME_FULL_NATIVE_DONE...$')` without `re.MULTILINE` and called `.findall()` on the entire multi-line output. As a result all 256 `completion_count` were wrongly zero and CSV marked all FAIL, notwithstanding each raw trace. This is **the baseline result parser**, not DUT behavior, and the original invalid summary must be preserved. Correct only the parsing step to search each log line with `fullmatch` and require exactly one completion; keep all 256 same payloads, waveform checkpoints, accepted handshakes, upstream source commit, and symbolic proof unchanged. Then rerun and independently audit the raw results against the original failing artifact. This correction is retrospective and not a new hypothesis.