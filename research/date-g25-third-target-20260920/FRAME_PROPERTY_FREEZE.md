# G25D final property freeze — after legal 0xA5 timing trace, before symbolic evaluation (2026-09-20)

First exploratory trace 35529163373 invalid AXI master (VALID held step14 and withdrawn before a second ready pulse): one observed handshake but *two physical TX starts*, cannot infer DUT defect. Erratum committed prior to corrected run. Lawful rerun 35529261298 holds VALID only steps 12 and 13, sees one handshake step13, one serial start step16, eight 4-clock data cells steps20..51 and 4-clock stop 52..55, idle-high again after. These timings are selected based on lawful **single data value 0xA5**, hence NOT prospective discovery of timing; source design BAUD=4. Now freeze sampled centers for all symbolic bytes: step17 start=0; step21,25,29,33,37,41,45,49 respectively equal input byte bits0..7; step53 stop=1; step57 idle=1; exactly one AXI write accepted. All original `axiluart/ufifo/skidbuffer/txuart/rxuart` modules read with `read_verilog -sv` (FORMAL macro NOT defined); new harness alone uses formal assertions and anyconst 8-bit immutable payload. Run SBY smtbmc z3 BMC depth60, separate positive cover with sampled reconstructed byte and stop, and intentionally wrong bit0 comparison negative control expected FAIL. No source changes to upstream RTL, no forced internals for functional assertions, no claim about other bus schedules, parity, reset, CTS, data size, whole-device proof, or novelty of adding formal properties. If PASS is achieved it establishes a bounded exhaustive *one-byte input domain* only in fixed schedule. Do not multiply 256 equivalent symbolic values into 256 independent defects or claim the source review alone cannot predict successful behavior.