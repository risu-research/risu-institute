# G25D initial timing harness protocol erratum — written before legal rerun

The first original UART timing trace (workflow 35529163373) logged one simultaneous AXI AW/W handshake at step 13 but showed a second physical serial start at step 56. The exploratory testbench drove both AWVALID and WVALID during steps 12, 13, AND 14; after handshake at step 13, the still-asserted VALID at step14 can trigger a second ready pulse for step15. At step15 the testbench prematurely lowers VALID, violating AXI's rule to hold valid until accepted. Therefore the second frame is not evidence of a UART functional defect, and original single-write framing claims must NOT rely on this invalid trace. Correct the TESTBENCH ONLY by asserting both VALIDs exclusively at steps 12 and 13, so the request is withdrawn immediately following its observed handshake. Keep all original DUT sources unchanged; preserve first raw trace and compare native output of the lawful stimulus before sampling or symbolic proving. Correcting the master protocol after seeing anomalous data is an exploratory repair, not original preregistration.