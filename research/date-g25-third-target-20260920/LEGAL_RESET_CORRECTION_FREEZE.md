# G25C legal-master correction, retrospective, frozen before corrective run 2026-09-20

Earlier G25C physical-formal fixture `wire awvalid=(step>=12 && step<=14);` leaves AWVALID/WVALID high for step14 although the native reference shows their joint handshake at step13. Thus its asserted serial-low-under-reset check is **not** evidence for one legal AXI request. Preserve original raw G25C artifacts and identify the weakness. Change **testbench only**: both valids high only at steps12 and13, just as the previously corrected G25D lawful one-write reference; count simultaneous AW/W handshakes, assert count==1 from step15 through28 and at native completion, require serial low during reset step20..25. Separate cover of accepted==1, reset asserted and UART busy with line low at step20. Keep same upstream commit, unmodified DUT RTL, clock schedule, reset window step18..25, arbitrary byte anyconst and original status logs. Run bounded Z3 formal and 0x55 native simulation, retain failures and traces. Nothing here asserts the design should abort an in-flight frame on an AXI reset or demonstrates a novel historical defect. If proof fails with legal master, retract former G25C inference rather than retrofitting unrecorded assumptions.