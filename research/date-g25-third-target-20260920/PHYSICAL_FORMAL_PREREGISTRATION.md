# G25C distinct follow-on after G25A/B, frozen before G25C data, 2026-09-20

G25B original AXI write midframe reset observed physical TX low throughout eight reset cycles. We do not claim a defect or unspecified reset requirement. Third approach: instead of the original AXI proof with unconstrained line, **elaborate the unmodified original physical AXI-UART/TX/RX/FIFO sources without FORMAL macro**, then read only a new property harness with `read_verilog -formal -sv`. Check a fixed reset/one-word-write timeline and arbitrary 8-bit payload (all 256 values symbolically) and assert that the serial line is low through eight mid-frame reset cycles. Pin source commit d8ea86d1b8ef47d0b1c13f291a958b6aac9229f9. 26-32 step bounded prove only; separate cover the actual serial start and reset condition. Formal error or unsupported Yosys syntax is not a PASS; retain all logs and source hashes. A local proof under a fixed schedule is not an unbounded all-UART guarantee, nor evidence that the original upstream AXI property was wrong. A source-level observation of tied-off reset is not a proof of its temporal effect, but this simple test alone may still be too little novelty for DATE. Do not promote unless a useful decision is independently established beyond source review and a few directed executions. This experiment is post-G25A/B, NOT preregistered at initial target selection.