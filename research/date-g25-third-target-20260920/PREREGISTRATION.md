# G25 frozen third-target experiment (2026-09-20, prior to execution)

Independent original source: ZipCPU/wbuart32 commit d8ea86d1b8ef47d0b1c13f291a958b6aac9229f9. Alternatives screened: ZipCPU/zipcpu G11 has already been explored at length; lowRISC/ibex full setup is costly; this wbuart32 revision provides an original axiluart formal task and a separate physical UART implementation. Selection uses only available original source and formal configuration; this is not discovery of a new defect.

Question: Does the original AXI-UART formal PASS entail idle-high physical serial TX after reset, with RX high and no AXI traffic? Run the untouched upstream `bench/formal/axiluart.sby prf` and retain its actual status. In the same original source with FORMAL set, check `o_uart_tx==1` at clock >=3 under reset-low clocks 0-1 then reset-high and no AXI writes; retain original VCD and status. With FORMAL absent, compile unchanged original AXI peripheral and UART transmitter, simulate the exact no-traffic reset stimulus for 16+ clocks and record TX line, no X values, plus compile/runtime status. `FORMAL` is expected to replace real TX with unconstrained TX, but expected outcomes are predictions, NOT results. Preserve all failed attempts, hashes, source and raw logs; invalid/timeout does not count as PASS. This is a proof-scope (not product-defect) experiment.

Promotion gate: add to paper only if original task PASS, serial claim formal FAIL with genuine countermodel, native original idle-high PASS, and comparison to ordinary source review and directed test yields a **decision not already obvious from them**. If only source stub plus trivial countermodel, archive negative value-add, leave G23 manuscript unchanged. No historic CI claims without evidence, no first-to-discover claim, no all-UART-frame or silicon claim. If first strategy weak, independently re-evaluate G11 ZipCPU and an Ibex small-target feasibility screen; never retrofit preregistration.