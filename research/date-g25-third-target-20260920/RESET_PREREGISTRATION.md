# G25B post-G25 exploratory second attack, frozen before G25B data, 2026-09-20

G25A stock AXI proof PASS, abstracted UART-line idle claim FAIL, original native UART idle PASS; a source reader can see the abstraction, so G25A does NOT satisfy the paper promotion gate. This is a **distinct exploratory follow-up**, not a prospectively preselected independent design and not a third bug.

Use exact unchanged original wbuart32 commit d8ea86d1b8ef47d0b1c13f291a958b6aac9229f9, full `axiluart.v` + original TX/RX/FIFOs, and an actual AXI-lite TX register write to enqueue a byte with a nontrivial start/data pattern. After observing physical serial start-low, assert external AXI reset for at least four clock cycles without new writes; observe serial pin and AXI handshakes. Hold no other traffic. Hypothesis (not data): AXI reset may leave a previously initiated physical serial frame alive because top connects `txuart` reset to 1'b0, despite original AXI proof PASS; this is not automatically a device defect without a documented requirement to abort a frame. Record timing, signal, original source, failures and complete log. An original source-visible `i_reset=1'b0` *alone* does not meet unique method value-add. If simulation shows continuation and status is not a new actionable inference beyond source and simulation, archive as a negative research result, do NOT add to DATE paper. No claim about UART electrical silicon. Proceed to separate third tactic if weak.