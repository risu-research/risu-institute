# G27 — fair decision baseline, frozen 2026-09-20 BEFORE NEW RTL RUN

Question: Does the G23/G26 source/configuration/legality/initial-state auditing procedure justify a practical conclusion unavailable to an ordinary well-informed source inspection, minimal native test, or appropriately configured existing formal check? No claim of novel algorithm, new bug, speedup, human-effort improvement or prospective blind discovery is allowed.

Independent experimental baseline: unmodified historical olofk/serv RF memory-interface module (parent 838d16615e81786062fae84b9f7c044fb8f6bbe4 and repaired 41e8aeedfd1e9ad5f95902c5b0dfc83d1c99e5d2). For each revision and rf_regs=32 (CSR-off) / rf_regs=36 (CSR-on): a conventional Icarus simulation exhaustively exercises register index 0..31, byte lane 0..3, and stable arbitrary SRAM returned byte 0..255 (32768 physical RTL checks per configuration, total 131072). Explicitly drive/read the original HDL rather than a hand-written reimplementation. At every rising clock edge, input address and SRAM byte stable at least a half cycle; sample output after nonblocking assignments. Independent reference: register 0 must return zero; registers 1..31 must return the supplied byte. Verify a sampled positive control x30. Capture failures and their precise register/lane/payload and count by condition. Predict BEFORE execution: old/CSR-off: x0 4*255 mismatches and x31 4*255 mismatches => 2040; old/CSR-on x0 1020 and x31 zero =>1020; repaired both settings zero. If compilation/simulator or reference-model errors occur retain full failed run, fix fixture only and rerun with erratum. Do not claim this exhaustive one-read interface is an exhaustive CPU/SoC check, nor count the 131072 cells as independent defects.

Matched decision ladder using already archived original experiments (G20 PicoRV32, G23 SERVing, G25 UART and G26 input-owned robustness), with source/stimulus/observations disclosed and no artificial handicap for the baseline: (A) read original formal filesets and elaborated parameters before any runtime, (B) ordinary minimal legal original RTL stimulus, (C) existing stock checkers with correctly qualified input and actual counterexamples, (D) additional exact cover and source-linked scope audit. Predeclare outcomes separately for six decisions: whether default PicoRV32 PASS transfers to RV32E; whether old legal SLLI shamt16 has a concrete fault; whether repaired SLLI has a bounded target property; whether old SERVing CSR-on is x0-safe under arbitrary backing byte; whether stock SERV formal includes the changed RF module; and whether stock AXI-UART formal includes physical TX. Record the cheapest sufficient evidence stage for each, including `unknown` where appropriate. No numeric comparison of subjective human effort, selection independence, or generality. State clearly that the native exhaustiveness concerns only 8-bit returned SRAM values and one-read stable-byte model, exactly matching G23 isolated formal domain.

Decision: promote to paper only if the fair baseline demonstrates a generalizable NEW design decision or reproducible evaluation facility with substantive methodological advantage, not by number of cells or formal proofs. If native exhaustive baseline matches formal on the entire small domain, retain as negative control/report and do not pad DATE main paper. Any G26 benefit is robustness of assumption placement, not new domain or proof method; G26 uses COMPRESSED_ISA=0 versus G23 COMPRESSED_ISA=1.
