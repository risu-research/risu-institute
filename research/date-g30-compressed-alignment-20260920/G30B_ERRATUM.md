# G30B corrective protocol, recorded before G30B run
First G30 parent and child all three instruction-specific proofs FAILED, and all 12 exact covers succeeded. In repaired SLLI counterexample at cycle 15 the accepted instruction memory response was `0x01ff9f93` (rd x31, rs1 x31, syntactically SLLI), but G30 assumption checked `!uut.mem_la_firstword && !uut.mem_la_secondword`. The current lookahead `mem_la_firstword` was 1 while the accepted bus word carried a word-aligned instruction, so the condition was false. That means G30's input legality rule did not cover the memory response. The G30 FAIL does not by itself demonstrate a corrected CPU defect. Freeze the correction: apply the same register legality bits to every accepted *instruction memory response* whose full 32-bit data looks like a chosen shift, regardless of lookahead or split flags; apply the split rs1 restriction when the buffered lower half plus incoming upper half resemble a split shift. The upper-half candidate rule and compressed C.SLLI restriction are unchanged. This is a conservative memory-response language policy: it can constrain non-executed byte patterns, unlike the exact semantic set of all RV32E instructions. Proof and exact cover MUST share the same byte-identical effective wrapper, source SHA, CPU, checker and COMPRESSED_ISA=1. Preserve all original G30 traces; do not turn G30B PASS into full ISA proof. Compare shamt 15 child and shamt 16 both. Negative control G30 original unqualified child failure is separately available from earlier G20/G26.