# G30C frozen fallback BEFORE run
G30 failed due lookahead-gated input miss. G30B fixed the miss by restricting accepted bus memory response candidate patterns but still failed corrected CPU with an expanded compressed instruction: externally observed 32-bit response candidates are NOT a complete characterization of instructions after prefetch, halfword assembly, and decompression in the original unconstrained-memory model. Do not label either first strategy as successful or as evidence of a new CPU bug. This G30C fallback tests a distinct, deliberately weaker claim at the DUT's instruction-assembly boundary: before decode, when `uut.mem_done && (uut.mem_do_prefetch || uut.mem_do_rinst)`, constrain the fully assembled `uut.mem_rdata_latched` if 32-bit SLLI/SRLI/SRAI operand high registers are present, and constrain compressed C.SLLI rd/rs1 bit 4. Preserve COMPRESSED_ISA=1, RV32E, BARREL_SHIFTER=1, identical original parent and child and same 20-cycle original instruction check and identical effective wrapper for exact covers. This is a DUT-derived **predecode instruction** assumption, not an entirely environment-owned input assumption, though independent of `rvfi_insn`. The original G30 and G30B failures and all cover traces stay archived. If fixed still fails, record failure rather than patching CPU or classifying as a discovered bug. Covers x2,x9 shift15/16 with check, valid, spec_valid, !trap. Do not claim whole ISA or equivalence to external input legality.