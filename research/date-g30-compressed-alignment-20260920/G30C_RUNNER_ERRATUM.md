# G30C runner erratum (before rerun)
The first G30C workflow did not run any formal task. An overly broad Python self-check inspected the *driver source* for the textual string `assume (!rvfi_insn`, which was present in the driver's own embedded self-check even though the generated SystemVerilog rule had no RVFI assumption. All six jobs archived `script_exit=1`; no PASS/FAIL formal outcome is claimed. G30C second run changes only that guard to inspect `qual` (actual generated SystemVerilog rule) rather than the Python driver `fixed`, while preserving the pre-run frozen rule and original DUT. This is a runner correction, not an experiment result.