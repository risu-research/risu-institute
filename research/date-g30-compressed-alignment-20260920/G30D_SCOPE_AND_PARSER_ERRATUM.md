# G30D source-local instrumentation / fundamental G30-family erratum — frozen before G30D run
The initial G30/G30B/G30C wrappers referenced `uut.mem_16bit_buffer`, `uut.mem_la_firstword`, `uut.mem_done`, or `uut.mem_rdata_latched` in their own Yosys module. The *real SymbiYosys logs* warn `Identifier \\uut.mem_rdata_latched is implicitly declared`, out-of-bounds selections, likewise mem_done and mem_do_rinst. Therefore these hierarchical references did NOT constrain the intended internal CPU signals. All past G30-family FAIL results are **invalid evidence against even an input restriction**, and their PASS exact covers do not establish matched assumptions. Preserve their raw archives and do not call repaired CPU buggy. Merely adjusting those assumptions while leaving implicitly declared wires is forbidden.

G30D directly inserts a small `ifdef RISCV_FORMAL` assumption-only probe INSIDE the historical PicoRV32 module, after the original `assign mem_rdata_latched` and before the next original always block. All other original RTL remains byte-identical before/after subtracting the single instrument block; no functional assignment/always is altered. Real `mem_done` and assembled `mem_rdata_latched` are in scope; use 32-bit SLLI/SRLI/SRAI rd/rs1 x0..15 and C.SLLI rd x0..15, no shift-amount restriction. The probe remains at the **DUT predecode/assembled instruction boundary**, NOT entirely external/environment-owned nor equivalent to arbitrary accepted bus bytes. Scope is COMPRESSED_ISA=1, ENABLE_REGS_16_31=0 and BARREL_SHIFTER=1 in the unchanged original wrapper plus register parameter; parent/child x three operations. Identical wrapper, instrumented source, RISCV_FORMAL macros and assumptions for original 20-cycle proof and exact named cover of shamt 16 both versions and 15 repaired, optional halfword PC cover. Check full formal source SHA, original CPU blob, warnings, formal statuses and cover VCD. Added internal probe may not be named a new RTL defect, generalized input legality, or a source-unmodified hardware proof: functional logic unchanged but only formal clone has assertion-only source instrumentation.

If exact cover and proof both PASS for repaired CPU and historical old FAIL under identical assumptions, report bounded, nonvacuous *predecode-qualified* proof boundary, not true input-port equivalence. If parent FAIL and repaired FAIL, preserve as FAIL and evaluate counterexample/probe linkage. Do not revise DATE paper until independent raw-audit and academic framing.